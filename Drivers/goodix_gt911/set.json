{
  "address": { "default": "0x5D" },
  "poll_ms": { "default": 20 },
  "settings": [
    {
      "key":     "swap_xy",
      "label":   "Swap X/Y axes",
      "type":    "bool",
      "default": false,
      "comment": "Orientation (applied by the display handler, not the chip). Transpose: use the touch X as the display Y and vice versa — for a panel scanned 90 deg rotated vs the display. Applied before mirror."
    },
    {
      "key":     "mirror_x",
      "label":   "Mirror X",
      "type":    "bool",
      "default": false,
      "comment": "Flip the X axis against the display width (after any swap)."
    },
    {
      "key":     "mirror_y",
      "label":   "Mirror Y",
      "type":    "bool",
      "default": false,
      "comment": "Flip the Y axis against the display height (after any swap)."
    }
  ]
}
